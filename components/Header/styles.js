import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
  header: {
    width: '100%',
    padding: 15,
    alignItems: 'Left',
  },
  headerText: {
    color: 'black',
    fontSize: 30,
    fontWeight: 'bold',
  },
});
